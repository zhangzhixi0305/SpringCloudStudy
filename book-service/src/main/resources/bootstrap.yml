spring:
  cloud:
    config:
      # 名称，从哪个服务去取配置（其实就是客户端项目的服务名称）
      name: bookerservice
      # 配置服务器的地址
      uri: http://localhost:8700
      # 环境
      profile: dev
      # 分支
      label: master